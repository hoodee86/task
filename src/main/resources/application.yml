spring:
  datasource:
    url: jdbc:mariadb://**.**.**.**:3306/task_engine
    username:
    password:
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
